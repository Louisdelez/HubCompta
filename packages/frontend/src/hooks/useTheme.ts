// ============================================================================
// THEME HOOK - Finance Hub
// Re-export from ThemeProvider for backward compatibility
// ============================================================================

// Re-export everything from ThemeProvider
export { useTheme, type ThemeMode, type CatppuccinFlavor, type Theme, type AutoThemeConfig } from '@/providers/ThemeProvider';
export { default } from '@/providers/ThemeProvider';
